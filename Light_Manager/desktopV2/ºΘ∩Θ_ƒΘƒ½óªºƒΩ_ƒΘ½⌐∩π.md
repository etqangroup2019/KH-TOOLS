# 🚀 دليل الاستخدام السريع - منظومة المهندس v3

## 🎯 البدء السريع

### 1. تشغيل النظام التفاعلي
```bash
python تشغيل_النظام_المحاسبي.py
```

### 2. استخدام النظام برمجياً
```python
from نظام_المحاسبة_الشامل import نظام_المحاسبة_الشامل

# إنشاء مثيل من النظام
نظام = نظام_المحاسبة_الشامل()

# إنشاء النظام الكامل (مرة واحدة فقط)
نظام.انشاء_النظام_الكامل()
```

## 📋 العمليات الأساسية

### إضافة عميل جديد
```python
معرف_العميل = نظام.ادراج_بيانات('العملاء', {
    'رقم_العميل': 'C001',
    'اسم_العميل': 'شركة الرياض للتطوير',
    'نوع_العميل': 'شركة',
    'رقم_الهوية_السجل': '1234567890',
    'العنوان': 'الرياض، حي الملك فهد',
    'رقم_الهاتف': '011-1234567',
    'البريد_الإلكتروني': 'info@riyadh-dev.com',
    'الحالة': 'نشط',
    'المستخدم': 'admin'
})
```

### إضافة موظف جديد
```python
معرف_الموظف = نظام.ادراج_بيانات('الموظفين', {
    'رقم_الموظف': 'E001',
    'اسم_الموظف': 'أحمد محمد السعيد',
    'رقم_الهوية': '1234567890',
    'الوظيفة': 'مهندس مشاريع',
    'القسم': 'الهندسة',
    'تاريخ_التوظيف': '2024-01-01',
    'المرتب_الأساسي': 8000,
    'الحالة': 'نشط',
    'المستخدم': 'admin'
})
```

### إضافة مشروع جديد
```python
معرف_المشروع = نظام.ادراج_بيانات('المشاريع', {
    'رقم_المشروع': 'P001',
    'معرف_العميل': معرف_العميل,
    'معرف_المدير': معرف_الموظف,
    'اسم_المشروع': 'مجمع سكني - حي النرجس',
    'الوصف': 'إنشاء مجمع سكني متكامل',
    'الموقع': 'الرياض، حي النرجس',
    'المساحة': 5000,
    'قيمة_المشروع': 2500000,
    'تاريخ_البداية': '2024-01-15',
    'تاريخ_النهاية_المخطط': '2024-12-31',
    'الحالة': 'قيد_التنفيذ',
    'المستخدم': 'admin'
})
```

### إنشاء قيد محاسبي
```python
تفاصيل_القيد = [
    {
        'معرف_الحساب': 1,  # حساب النقدية
        'البيان': 'نقدية',
        'مدين': 1000.00,
        'دائن': 0.00
    },
    {
        'معرف_الحساب': 2,  # حساب رأس المال
        'البيان': 'رأس المال',
        'مدين': 0.00,
        'دائن': 1000.00
    }
]

معرف_القيد = نظام.انشاء_قيد_محاسبي(
    "قيد افتتاحي - رأس المال",
    تفاصيل_القيد,
    'افتتاحي'
)
```

## 📊 الاستعلامات والتقارير

### عرض إحصائيات النظام
```python
stats = نظام.احصائيات_النظام()
print(stats)
```

### حساب رصيد حساب
```python
رصيد = نظام.حساب_رصيد_حساب(معرف_الحساب=1)
print(f"رصيد الحساب: {رصيد:,.2f}")
```

### تحميل بيانات
```python
# تحميل جميع العملاء النشطين
عملاء = نظام.تحميل_بيانات(
    'العملاء', 
    'id, اسم_العميل, رصيد_العميل', 
    'الحالة = %s', 
    ('نشط',)
)

# تحميل المشاريع الجارية
مشاريع = نظام.تحميل_بيانات(
    'المشاريع',
    '*',
    'الحالة = %s',
    ('قيد_التنفيذ',)
)
```

## 🔧 العمليات المتقدمة

### إنشاء نسخة احتياطية
```python
نظام.انشاء_نسخة_احتياطية('backup_2024.sql')
```

### استعادة نسخة احتياطية
```python
نظام.استعادة_نسخة_احتياطية('backup_2024.sql')
```

### تحديث بيانات
```python
نظام.تحديث_بيانات(
    'العملاء',
    {'رصيد_العميل': 5000},
    'id = %s',
    (معرف_العميل,)
)
```

### حذف بيانات
```python
نظام.حذف_بيانات(
    'المشاريع',
    'id = %s AND الحالة = %s',
    (معرف_المشروع, 'ملغي')
)
```

## 🏗️ هيكل الجداول الرئيسية

### جداول النظام الأساسية
- `إعدادات_النظام` - إعدادات النظام العامة
- `المستخدمين` - بيانات المستخدمين والصلاحيات
- `التصنيفات` - التصنيفات الموحدة
- `سجلات_النظام` - سجلات العمليات
- `المرفقات` - إدارة المرفقات

### جداول المحاسبة
- `شجرة_الحسابات` - الحسابات المحاسبية
- `القيود_المحاسبية` - القيود المحاسبية
- `تفاصيل_القيود_المحاسبية` - تفاصيل القيود
- `مراكز_التكلفة` - مراكز التكلفة
- `السنوات_المالية` - السنوات المالية
- `أرصدة_الحسابات` - أرصدة الحسابات

### جداول المشاريع
- `المشاريع` - المشاريع الرئيسية
- `مراحل_المشروع` - مراحل المشاريع
- `مهام_فريق_العمل` - مهام الفريق
- `عهد_مالية_المشاريع` - العهد المالية
- `دفعات_المشاريع` - دفعات المشاريع
- `مصروفات_المشاريع` - مصروفات المشاريع

### جداول الموارد البشرية
- `الموظفين` - بيانات الموظفين
- `العملاء` - بيانات العملاء
- `الموردين` - بيانات الموردين
- `حضور_وانصراف_الموظفين` - نظام الحضور
- `تقييم_الموظفين` - تقييمات الأداء
- `معاملات_الموظفين_المالية` - المعاملات المالية

### جداول التدريب
- `البرامج_التدريبية` - البرامج التدريبية
- `المجموعات_التدريبية` - المجموعات
- `المدربين` - بيانات المدربين
- `المتدربين` - بيانات المتدربين
- `المشتركين_في_التدريب` - المشتركين
- `الشهادات` - الشهادات المصدرة

## ⚠️ نصائح مهمة

### 1. الأمان
- استخدم كلمات مرور قوية
- قم بعمل نسخ احتياطية دورية
- راقب سجلات النظام

### 2. الأداء
- استخدم الفهارس المناسبة
- نظف البيانات القديمة دورياً
- راقب حجم قاعدة البيانات

### 3. الصيانة
- تحديث الإحصائيات دورياً
- فحص سلامة البيانات
- مراجعة السجلات

## 🆘 استكشاف الأخطاء

### مشاكل الاتصال
```python
# اختبار الاتصال
if نظام.فحص_الاتصال():
    print("الاتصال سليم")
else:
    print("مشكلة في الاتصال")
    نظام.اعادة_الاتصال()
```

### مشاكل البيانات
```python
# التحقق من وجود بيانات
بيانات = نظام.تحميل_بيانات('العملاء', 'COUNT(*) as count')
if بيانات and بيانات[0]['count'] > 0:
    print("توجد بيانات")
else:
    print("لا توجد بيانات")
```

## 📞 الدعم

للحصول على المساعدة:
1. راجع ملف `README_النظام_المحاسبي.md`
2. تحقق من سجلات النظام في `accounting_system.log`
3. استخدم الواجهة التفاعلية للاختبار

---

**تم إنشاء هذا الدليل لمنظومة المهندس v3**
*آخر تحديث: ديسمبر 2024*