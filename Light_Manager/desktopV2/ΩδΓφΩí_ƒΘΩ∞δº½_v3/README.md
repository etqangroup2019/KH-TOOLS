# منظومة المهندس v3 - نظام المحاسبة الشامل

## نظرة عامة
منظومة المهندس v3 هو نظام محاسبي شامل ومتكامل مصمم خصيصاً للشركات الهندسية والمقاولات. يوفر النظام إدارة كاملة لجميع العمليات المالية والإدارية مع دعم كامل للغة العربية.

## الميزات الرئيسية
- 🏗️ **نظام وحدات قابل للتوسع**: هيكل مرن يسمح بإضافة وحدات جديدة بسهولة
- 🗄️ **قاعدة بيانات متقدمة**: دعم MySQL مع ترميز UTF-8 كامل للعربية
- 📊 **تقارير شاملة**: نظام تقارير متقدم لجميع العمليات
- 🔒 **أمان عالي**: نظام صلاحيات وتشفير متقدم
- 🌐 **دعم كامل للعربية**: واجهة وقاعدة بيانات باللغة العربية

## هيكل المشروع

```
منظومة_المهندس_v3/
├── core/                           # النواة الأساسية للنظام
│   ├── __init__.py
│   ├── database.py                 # مدير قاعدة البيانات
│   ├── base_module.py             # الكلاس الأساسي للوحدات
│   └── system_manager.py          # مدير النظام الرئيسي
│
├── modules/                        # وحدات النظام
│   ├── accounting/                 # وحدة المحاسبة
│   │   ├── __init__.py
│   │   └── accounting_module.py
│   │
│   ├── projects/                   # وحدة المشاريع
│   │   ├── __init__.py
│   │   └── projects_module.py
│   │
│   ├── clients/                    # وحدة العملاء
│   │   ├── __init__.py
│   │   └── clients_module.py
│   │
│   ├── employees/                  # وحدة الموظفين
│   │   ├── __init__.py
│   │   └── employees_module.py
│   │
│   ├── suppliers/                  # وحدة الموردين
│   │   ├── __init__.py
│   │   └── suppliers_module.py
│   │
│   ├── contracts/                  # وحدة المقاولات
│   │   ├── __init__.py
│   │   └── contracts_module.py
│   │
│   ├── training/                   # وحدة التدريب
│   │   ├── __init__.py
│   │   └── training_module.py
│   │
│   ├── expenses/                   # وحدة المصروفات
│   │   ├── __init__.py
│   │   └── expenses_module.py
│   │
│   ├── revenues/                   # وحدة الإيرادات
│   │   ├── __init__.py
│   │   └── revenues_module.py
│   │
│   └── reports/                    # وحدة التقارير
│       ├── __init__.py
│       └── reports_module.py
│
├── config/                         # ملفات الإعدادات
│   └── settings.py
│
├── tests/                          # اختبارات النظام
│   ├── test_basic.py
│   ├── test_modules.py
│   └── run_test.py
│
├── docs/                           # التوثيق
│   ├── api/                        # توثيق API
│   ├── user_guide/                 # دليل المستخدم
│   └── developer_guide/            # دليل المطور
│
├── scripts/                        # سكريبتات مساعدة
│   ├── backup.py                   # نسخ احتياطي
│   ├── restore.py                  # استعادة البيانات
│   └── migration.py                # ترحيل البيانات
│
├── run_system.py                   # تشغيل النظام الكامل
├── نظام_المحاسبة_الشامل.py         # الملف الأصلي (للمرجع)
└── README.md                       # هذا الملف
```

## الوحدات المتاحة

### 1. 📦 وحدة المحاسبة (accounting)
- **الوصف**: النظام المحاسبي الأساسي
- **الوظائف**: دليل الحسابات، القيود، مراكز التكلفة
- **الجداول**: 15+ جدول محاسبي

### 2. 📦 وحدة المشاريع (projects)
- **الوصف**: إدارة المشاريع الهندسية
- **الوظائف**: تتبع المشاريع، المراحل، التكاليف
- **الجداول**: 10 جداول

### 3. 📦 وحدة العملاء (clients)
- **الوصف**: إدارة العملاء والمعاملات
- **الوظائف**: بيانات العملاء، الفواتير، المدفوعات
- **الجداول**: 3 جداول

### 4. 📦 وحدة الموظفين (employees)
- **الوصف**: إدارة الموارد البشرية
- **الوظائف**: بيانات الموظفين، الرواتب، الحضور
- **الجداول**: 7 جداول

### 5. 📦 وحدة الموردين (suppliers)
- **الوصف**: إدارة الموردين والمشتريات
- **الوظائف**: بيانات الموردين، الفواتير، المدفوعات
- **الجداول**: 4 جداول

### 6. 📦 وحدة المقاولات (contracts)
- **الوصف**: إدارة عقود المقاولات
- **الوظائف**: العقود، المراحل، الدفعات
- **الجداول**: 4 جداول

### 7. 📦 وحدة التدريب (training)
- **الوصف**: إدارة البرامج التدريبية
- **الوظائف**: البرامج، المتدربين، الشهادات
- **الجداول**: 7 جداول

### 8. 📦 وحدة المصروفات (expenses)
- **الوصف**: إدارة المصروفات العامة
- **الوظائف**: تسجيل وتتبع المصروفات
- **الجداول**: 3 جداول

### 9. 📦 وحدة الإيرادات (revenues)
- **الوصف**: إدارة الإيرادات والدخل
- **الوظائف**: تسجيل وتتبع الإيرادات
- **الجداول**: 2 جدول

### 10. 📦 وحدة التقارير (reports)
- **الوصف**: التقارير والتحليلات
- **الوظائف**: تقارير مالية، إحصائيات، تحليلات
- **الجداول**: 4 جداول

## متطلبات النظام

### متطلبات البرمجيات
- Python 3.8+
- MySQL 8.0+
- mysql-connector-python

### متطلبات الأجهزة
- ذاكرة: 4GB RAM (الحد الأدنى)
- مساحة القرص: 10GB (للبيانات والنسخ الاحتياطية)
- معالج: Intel i3 أو ما يعادله

## التثبيت والإعداد

### 1. تثبيت المتطلبات
```bash
pip install mysql-connector-python
```

### 2. إعداد قاعدة البيانات
```sql
CREATE DATABASE منظومة_المهندس_v3 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;
```

### 3. تشغيل النظام
```bash
python run_system.py
```

## كيفية إضافة وحدة جديدة

### 1. إنشاء مجلد الوحدة
```bash
mkdir modules/new_module
```

### 2. إنشاء ملفات الوحدة
```python
# modules/new_module/__init__.py
from .new_module import NewModule
__all__ = ['NewModule']

# modules/new_module/new_module.py
from core.base_module import BaseModule

class NewModule(BaseModule):
    def __init__(self, db_manager):
        super().__init__(db_manager, "اسم_الوحدة")
        self.tables = ['جدول1', 'جدول2']
    
    def create_tables(self):
        # إنشاء الجداول
        pass
    
    def get_module_info(self):
        return {
            'اسم_الوحدة': self.module_name,
            'الوصف': 'وصف الوحدة',
            'الإصدار': '1.0.0',
            'الجداول': self.tables
        }
```

### 3. تسجيل الوحدة في النظام
أضف الوحدة إلى قائمة `modules_to_import` في `core/system_manager.py`

## الاختبار

### اختبار أساسي
```bash
python test_basic.py
```

### اختبار الوحدات
```bash
python test_modules.py
```

### اختبار شامل
```bash
python run_test.py
```

## التطوير والمساهمة

### هيكل الكود
- اتبع معايير PEP 8 للكود Python
- استخدم التعليقات باللغة العربية
- اتبع نمط التسمية العربي للمتغيرات والدوال

### إضافة ميزات جديدة
1. أنشئ فرع جديد للميزة
2. اكتب الاختبارات أولاً
3. طور الميزة
4. اختبر الكود
5. أرسل طلب دمج

## الأمان

### حماية قاعدة البيانات
- استخدم كلمات مرور قوية
- فعل SSL للاتصالات
- قم بعمل نسخ احتياطية دورية

### حماية التطبيق
- تحقق من صحة جميع المدخلات
- استخدم معاملات محضرة للاستعلامات
- سجل جميع العمليات الحساسة

## الدعم والمساعدة

### التوثيق
- دليل المستخدم: `docs/user_guide/`
- دليل المطور: `docs/developer_guide/`
- توثيق API: `docs/api/`

### الأخطاء الشائعة
1. **خطأ الاتصال بقاعدة البيانات**: تحقق من إعدادات الاتصال
2. **خطأ الترميز**: تأكد من استخدام UTF-8
3. **خطأ الصلاحيات**: تحقق من صلاحيات المستخدم

## الترخيص
هذا المشروع مرخص تحت رخصة MIT. راجع ملف LICENSE للتفاصيل.

## تاريخ الإصدارات

### v3.0.0 (الحالي)
- إعادة هيكلة كاملة للنظام
- نظام وحدات قابل للتوسع
- 9 وحدات أساسية
- 44+ جدول قاعدة بيانات

### v2.0.0
- النظام الأصلي المتكامل

---

**تم تطوير هذا النظام بواسطة فريق منظومة المهندس**

للمزيد من المعلومات أو الدعم الفني، يرجى التواصل معنا.